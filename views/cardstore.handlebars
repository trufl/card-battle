{{!-- <section class="card">
  <img src="/images/{{card.image}}" alt="{{painting.description}}">
  <div class="painting-details">
    <h3>{{painting.title}}</h3>
    <p>By: {{painting.artist}}</p>
    <p>Exhibition open until: {{ format_date painting.exhibition_date}}</p>
  </div>
</section> --}}
<section class="card">
  <!-- The contents of this card links to the gallery page using the gallery ID -->
  <a href="/card/{{id}}">
    <!-- The card contains the gallery name as the card header -->
    <header>Cards</header>
    <!-- The card contains the first painting of the gallery -->
    <img src="/images/{{cards.filename}}" >
  </a>
</section>

<section class="card">
  <img src="/images/{{card.filename}}">
  <div class="card-details">
    <h3>card 2</h3>
  </div>
</section>


{{!-- <div>
  {{if card.id = user.deck.card.id}}
</div> --}}


{{!-- maybe use with for getting ussers current hand 
 {{#with user}}
{{deck}}
{{/with}} --}}