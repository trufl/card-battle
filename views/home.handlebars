<section class="min-h-screen w-screen bg-indigo-700 flex justify-center content-center relative pb-28 pt-32">
    <section class="p-10 border-4 border-black bg-orange-600 h-fit">
        <a href="/signup">
            <button class="btn-game">
                Sign Up
            </button>
        </a>
        {{#if logged_in}}
        <div></div>
        {{else}}
        <a href="/login">
            <button class="btn-game">
                Login
            </button>
        </a>
        {{/if}}
        {{#if logged_in}}
        <a href="/battle">
            <button class="btn-game">
                Battle as {{{username}}}
            </button>
        </a>
        <a href="/customize">
            <button class="btn-game">
                Edit Deck {{{username}}}
            </button>
        </a>
        {{else}}
        <a href="/battle">
            <button class="btn-game">
                Battle as Guest
            </button>
        </a>
        {{/if}}
    </section>
</section>